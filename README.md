# Bioinformatics HW
## Третьякова Юля Б06-303

На данный момент сделано и загружено 6 домашних заданий по биоинформатике. Здесь доступно их краткое описание.

## Задание 1

Часть 1 (Linux и Git): создан публичный репозиторий, добавлен README и коммит Init; создана структура linux_task/data/raw, файлы sample1.tsv/sample2.tsv/sample3.tsv и заголовок с табуляцией; сформированы sequencing_summary.txt и samples_list.txt с подсчётом строк; отработаны ветки и восстановление файла через Git, сохранена история команд.

Часть 2 (R): создана папка r_task с двумя скриптами. first_plot.R формирует таблицу exp_data, выводит её структуру и сохраняет столбчатую диаграмму экспрессии в expression_plot.png. analyze_data.R читает sample_data.csv, выводит среднее Score и максимальный Score в группе Treatment и сохраняет boxplot по группам в score_boxplot.png.

## Задание 2

Часть 1 (Множественное выравнивание): выбраны гомологичные белки из 4 разных биологических видов, получены их последовательности из UniProt, выполнено множественное выравнивание, построено филогенетическое дерево по Newick-формату и подготовлены изображения выравнивания и дерева с перечислением использованных видов.

Часть 2 (MEGA и коронавирус): для предоставленных последовательностей коронавируса выполнено множественное выравнивание и построено филогенетическое дерево в программе MEGA; затем в анализ добавлены 3 последовательности из других штаммов, повторно построены выравнивание и дерево, добавленные последовательности выделены на схеме и сделаны выводы по их расположению.

Часть 3.1 (BLAST, неизвестный белок): для неизвестной белковой последовательности проведён поиск по базе данных с помощью BLAST, определена предполагаемая функция белка, проверено наличие ортолога у человека и найден наиболее близкий человеческий белок, связанный с соответствующим нарушением зрения.

Часть 3.2 (BLAST, люцифераза): выполнен поиск 10 лучших кандидатов на белковую последовательность люциферазы в транскриптоме Photinus pyralis и/или Ignelater luminosus: из белковых последовательностей создана BLAST-база, найдены лучшие хиты, отобраны 10 кандидатов и сохранены в отдельный fasta-файл.

## Задание 3

Часть 1 (сравнение геномов и получение SNP): для геномов *E. coli* K-12 и *E. coli* O157:H7 выполнено попарное выравнивание с помощью MUMmer/Nucmer, получены файлы `.delta`, `.coords` и `.snps`, выведена шапка SNP-файла и разобрана структура его столбцов. Для трёх вариантов фильтрации построены графики распределения и графики плотности SNP по геному, после чего сделаны выводы о характере различий между штаммами.

Часть 2 (аннотация SNP): SNP-файл был загружен в Python, из него были отобраны только однонуклеотидные замены, после чего загружена аннотация референсного генома *E. coli* K-12 из GenBank. Для каждой SNP определено положение относительно CDS, а для замен внутри кодирующих областей вычислены тип эффекта (synonymous, nonsynonymous, nonsense, stop_lost), затронутый кодон и аминокислотная замена. Дополнительно построена диаграмма распределения типов SNP, график плотности SNP с наложением позиций CDS и составлена таблица топ-10 генов с наибольшим числом SNP.

Часть 3 (функционально значимые SNP в генах устойчивости к антибиотикам): выбран вариант анализа мутаций в генах, связанных с антибиотикорезистентностью. На основе статей по *E. coli* O157:H7 была собрана панель генов-кандидатов и набор ключевых слов для отбора белков, связанных с эффлюксом и устойчивостью к антибиотикам. Из общей таблицы SNP были выделены потенциально значимые варианты (`nonsynonymous`, `nonsense`, `stop_lost`), после чего гены-кандидаты сгруппированы по предполагаемым механизмам устойчивости, составлены итоговые таблицы и построена диаграмма распределения найденных SNP по этим механизмам.

## Задание 4

Часть 1 (сборка с Velvet): для paired-end FASTQ-файлов `7_S4_L001_R1_001.fastq` и `7_S4_L001_R2_001.fastq` выполнены de novo сборки в Velvet с разными значениями k-mer. Был подготовлен SLURM-скрипт, созданы папки результатов `k11`, `k21` и `k31`, проверено наличие файлов `contigs.fa`. По логам Velvet были выписаны первичные показатели сборок: число узлов графа, N50, максимальная длина контига и общая длина сборки.

Часть 2 (сравнение Velvet и SPAdes): на тех же исходных данных выполнена базовая сборка в SPAdes с параметром `--careful`, после чего сборки Velvet и SPAdes были сравнены через QUAST. В отчёт добавлена таблица из `report.html`, разобраны основные метрики качества сборки: `# contigs`, `Largest contig`, `Total length`, `N50`, `N90`, `L50`, `L90` и `GC (%)`. По результатам сравнения сделан вывод, что среди базовых сборок лучше всего выглядит SPAdes, так как у него меньше контигов, больше `Largest contig` и выше `N50`.

Часть 3 (улучшение сборки): сначала была сделана попытка улучшить сборки за счёт изменения параметров: для Velvet использовались `-exp_cov auto` и `-cov_cutoff auto`, а для SPAdes был запущен отдельный улучшенный вариант. Затем была сделана более осмысленная попытка: для Velvet использованы corrected reads, полученные после коррекции ошибок в SPAdes, а для SPAdes запущен режим `--isolate`. После этого через QUAST сравнены 4 сборки: старый Velvet, Velvet на corrected reads, старый SPAdes и SPAdes isolate. По итогам показано, что Velvet после corrected reads стал менее фрагментированным, но лучшей общей сборкой по `N50` и `Largest contig` остался базовый SPAdes.

## Задание 5

В работе был выполнен контроль качества FASTQ-данных из проекта PRJEB84057. Сначала через ENA/SRA Explorer были выбраны и скачаны 4 single-end FASTQ-файла, затем для исходных данных был запущен FastQC и собран общий отчёт MultiQC. По первому отчёту были оценены основные показатели качества: качество по позициям, доля N, GC-состав, длины ридов, дупликация, сверхпредставленные последовательности и наличие адаптеров.

Далее был выполнен тримминг данных с помощью fastp через SLURM-скрипт. Использовались auto-detection адаптеров, обрезка участков низкого качества с окном 5 и порогом качества 20, а также фильтр минимальной длины 36 нуклеотидов. После обработки были сохранены очищенные файлы `*_trimmed.fastq.gz`, а также HTML- и JSON-отчёты fastp.

После тримминга был повторно запущен FastQC для очищенных файлов и снова собран общий отчёт MultiQC. По результатам сравнения до/после показано, что адаптеры в данных не обнаружены, качество ридов было высоким уже до обработки, а основной эффект тримминга заключался в удалении слишком коротких ридов. При этом дупликация, особенности GC-состава и сверхпредставленные последовательности полностью не исчезли, так как они, скорее всего, связаны с особенностями библиотеки или эксперимента.

## Задание 5

Часть 1 (FastQC и MultiQC до тримминга): были выбраны и скачаны 4 single-end FASTQ-файла из проекта PRJEB84057. Для исходных данных был запущен FastQC, после чего все отчёты были объединены в один общий отчёт MultiQC. По нему были оценены основные показатели качества: качество по позициям, доля N, GC-состав, длины ридов, дупликация, сверхпредставленные последовательности и наличие адаптеров.

Часть 2 (тримминг): по результатам первого MultiQC было видно, что явной адаптерной контаминации нет, а качество ридов в целом хорошее. Тримминг был выполнен с помощью fastp через SLURM-скрипт. Использовались auto-detection адаптеров, обрезка участков низкого качества с окном 5 и порогом качества 20, а также фильтр минимальной длины 36 нуклеотидов. После обработки были сохранены очищенные файлы `*_trimmed.fastq.gz`, а также HTML- и JSON-отчёты fastp.

Часть 3 (контроль качества после тримминга): для очищенных файлов был повторно запущен FastQC и снова собран общий отчёт MultiQC. По сравнению до/после видно, что адаптеры в данных не обнаружены, качество ридов было высоким уже до обработки, а основной эффект тримминга заключался в удалении слишком коротких ридов. При этом дупликация, особенности GC-состава и сверхпредставленные последовательности полностью не исчезли, так как они, скорее всего, связаны с особенностями библиотеки или эксперимента.

## Задание 6

Часть 1 (анализ FastQC): для исходных paired-end ридов `Eg_Treg_S71_R1_001.fastq.gz` и `Eg_Treg_S71_R2_001.fastq.gz` был выполнен FastQC, а затем результаты объединены через MultiQC. По отчётам были оценены число ридов, GC-состав, качество по позициям, дупликация, переобогащённые последовательности и адаптеры. Основные проблемные моменты: повышенная дупликация, особенности GC-распределения, переобогащённые последовательности и небольшой адаптерный/технический сигнал.

Часть 2 (тримминг): риды были очищены с помощью `fastp` с автоматическим определением адаптеров и фильтрацией по качеству. После этого FastQC и MultiQC были запущены повторно. После тримминга сохранилось около 95.7% ридов, средняя длина уменьшилась с 76 bp до 74 bp, качество немного улучшилось, а адаптерный сигнал стал слабее. Для дальнейшего анализа были выбраны триммированные риды.

Часть 3 (выравнивание и подсчёт каунтов): очищенные риды были выровнены на GRCh38 с помощью STAR; получены BAM-файл и `RNA_ReadsPerGene.out.tab`. Уникально выровнялось 83.53% ридов. Затем StringTie в режиме de novo собрал `transcripts.gtf`, всего получилось 9727 транскриптов. HTSeq-count дал `htseq_counts.txt` с 62700 генами. Каунты STAR из 3-й колонки были сравнены с HTSeq-count: для 62700 общих генов построен scatter plot, корреляция на сырых каунтах составила 0.747, а после `log10(count + 1)` — 0.951. Значит, результаты STAR и HTSeq хорошо согласуются, хотя отдельные значения отличаются из-за разных правил подсчёта.

## Задание 7

Часть 1 (анализ дифференциальной экспрессии с DESeq2): для RNA-seq данных LuC были загружены сырые counts по генам и таблица metadata с группами образцов R и NR. После проверки соответствия образцов между counts и metadata для дальнейшего анализа были оставлены 57 общих образцов: 42 NR и 15 R. Низкоэкспрессируемые гены были отфильтрованы, после чего осталось 41371 ген. Для оценки структуры данных были построены PCA и clustermap по логарифмированным counts. По PCA и clustermap видно, что группы R и NR не разделяются полностью, а отдельные образцы могут отличаться от основной массы, но без дополнительных оснований они не удалялись. Далее были подготовлены таблицы counts и metadata для DESeq2, проведён анализ дифференциальной экспрессии R против NR, получена таблица с baseMean, log2FoldChange, p-value и adjusted p-value. По критерию padj < 0.05 и |log2FoldChange| >= 1 было найдено 190 значимых генов. Для визуализации результатов был построен volcano plot, по которому видно, что значимые гены распределены несимметрично и заметная часть наиболее значимых точек имеет отрицательный log2FoldChange.

Часть 2 (анализ дифференциальной экспрессии с LIMMA): для второй части был использован отдельный набор данных GSE63885, представленный как матрица экспрессии микрочипового эксперимента и таблица аннотации образцов. LIMMA был выбран, потому что этот метод подходит для анализа микрочиповой экспрессии и использует линейные модели для сравнения групп. В качестве групп сравнения были взяты pCR и pNC из клинической аннотации; в выбранной колонке группы распределены неравномерно: pCR = 39 и pNC = 2. Для анализа была построена design matrix, затем выполнены lmFit и eBayes. Были проверены разные пороги по модулю logFC: 1, 2 и 3. В итоговую таблицу добавлены отдельные столбцы significant для каждого порога. По критерию P.Value < 0.05 и выбранному порогу logFC найдено 67 значимых генов при logFC = 1, 4 гена при logFC = 2 и 0 генов при logFC = 3. Для каждого порога были построены volcano plot, а также график сравнения количества значимых генов при разных logFC. По результатам выбран порог logFC = 1, так как он остаётся достаточно строгим, но при этом сохраняет больше генов-кандидатов для дальнейшей интерпретации.
